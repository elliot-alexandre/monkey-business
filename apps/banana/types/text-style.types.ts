export type TextStyle<Override extends undefined | string = undefined> = {
    fontFamily?: string;
    fontSize: number;
    fontWeight?: number;
  };
  
  export type TextColor<Override extends undefined | string = undefined> = {
    color?: string;
  };
  